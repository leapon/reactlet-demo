# convert components

echo "build components ..."
cd public/component
./build-components

# build bundle files for distribution

cd ../..
echo "build bundle files ..."

browserify -o public/script/web/dist/index.js -d public/script/web/src/index.js

# end of build
echo "build is done"
