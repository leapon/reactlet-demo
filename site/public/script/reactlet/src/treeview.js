/** @jsx React.DOM */

var React = require('react');
var bootstrap = require('bootstrap');
var treeview = require('reactlet-treeview');

var app = app || {};

$().ready(function() {
    console.log('in reactlet treeview page');
});
